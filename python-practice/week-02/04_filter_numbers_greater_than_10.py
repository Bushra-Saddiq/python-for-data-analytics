# Task: Create a list of 5 numbers. Using a for loop, print only the
# numbers that are greater than 10.
# Topics: lists, for loop over a list, if inside a loop

numbers = [2, 4, 8, 11, 18]
for num in numbers:
    if num > 10:
        print(num)
